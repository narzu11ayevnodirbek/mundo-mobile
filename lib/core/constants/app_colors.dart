import 'package:flutter/material.dart';

class AppColors {
  // 🌿 Primary
  static const primary = Color(0xFF2ECC71);
  static const primaryDark = Color(0xFF27AE60);
  static const primaryLight = Color(0xFFA9F0C5);

  // 🌊 Secondary
  static const secondary = Color(0xFF3498DB);

  // 🌅 Accent (CTA / highlight)
  static const accent = Color(0xFFFFA726);

  // ⚪ Background
  static const background = Color(0xFFF8FAFC);
  static const card = Colors.white;

  // 🧾 Text
  static const textPrimary = Color(0xFF1E293B);
  static const textSecondary = Color(0xFF64748B);

  // ⚠️ Status
  static const warningBg = Color(0xFFFFF4E5);
  static const warning = Color(0xFFFFA726);

  // 🤖 AI Card
  static const aiDark = Color(0xFF0F2A44);

  // 🔲 Border
  static const border = Color(0xFFE2E8F0);

  static const doonyoColor = Color(0xFF0A1F44);
}
